from django.apps import AppConfig


class SocIncidentsConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "soc_incidents"
    verbose_name = "SOC Incident Management"

